import React from 'react';
import { LoaderContainer, LoaderSpinner } from './styled';

const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderSpinner />
    </LoaderContainer>
  );
};

export default Loader;
